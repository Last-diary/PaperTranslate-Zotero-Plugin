# Todo

1. [x] 增加 OpenAI Chat Completions 兼容基础客户端，并在其上实现 DeepSeek 专属适配器。
2. [x] 支持分别配置并保存 DeepSeek 与 OpenAI-compatible Provider；新增兼容服务时直接填写端点和模型，不兼容的协议再单独实现适配器。
3. [ ] 支持编辑并保存解析错误的内容。
4. [ ] 增加可配置的内容块过滤层，支持分别控制页眉、页脚、页码、脚注等块是否显示及是否参与翻译。
5. [ ] 支持可配置的 PDF 与右侧重排内容滚动同步（默认关闭）。实现前需确认目标 Zotero 版本中 PDF 当前页/滚动事件的稳定入口，并区分页级、块级与像素级同步的行为边界。
